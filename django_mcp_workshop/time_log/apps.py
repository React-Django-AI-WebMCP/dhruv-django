"""
App configuration for time_log.
"""

from django.apps import AppConfig


class TimeLogConfig(AppConfig):
    """Configuration for the time_log application."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "time_log"
